global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts/*.yml

scrape_configs:
  - job_name: "eventhub-api"
    static_configs:
      - targets: ["api:8080"]

  - job_name: "eventhub-worker"
    static_configs:
      - targets: ["worker:8081"] # worker health port
